#include "main.h"
/**
 * _islower - checks for lowercase character
 * @a: the character th be checked
 * Return: 1 for lowercase character or 0 for anything else
 */
int _islower(int a)
{
if (a >= 97 && a <= 122)
{
return (1);
}
else
{
return (0);
}
}

